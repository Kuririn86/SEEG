# 基于 SEEG 的癫痫发作状态智能检测算法解决方案

## 一、总体技术路线

本方案面向颅内立体脑电（stereo-electroencephalography, SEEG）多通道时序数据，构建由信号预处理、发作状态识别、发作起始点定位、关键通道解释和轻量化部署组成的端到端算法流程。系统输入为多通道 SEEG 片段，输出包括发作状态标签、发作概率、片段内发作起始时间以及贡献度最高的 10 个通道编号。整体设计以临床电生理特征为约束，以时序深度模型为主体，在保证检测性能的同时兼顾推理延迟、算力消耗和解释结果的稳定性。

算法流程分为离线训练和在线推理两个阶段。离线训练阶段首先对原始 SEEG 信号进行重采样、滤波、坏道剔除、参考重构和标准化处理，再按照固定时长和步长构建训练样本。模型采用时域卷积、时频增强、通道关系建模和长程时序依赖建模相结合的结构，学习发作期与发作间期在波形节律、频带能量、通道协同变化和时间演化过程上的差异。在线推理阶段按照与训练一致的预处理流程对输入片段进行滑窗分析，形成窗口级发作概率序列，经概率融合和后处理得到片段级二分类结果；若判定为发作期，则在概率序列上进行起始点定位，输出 onset time；同时根据模型内通道权重、梯度归因、遮挡检验和频带能量变化计算通道贡献度，输出 top-10 关键通道。

由于比赛尚未完全公开样本文件结构、采样率、通道命名规则和片段长度，本方案作如下合理假设：输入数据包含若干通道的连续 SEEG 信号及对应通道 ID；采样率可能位于 512 Hz 至 2 kHz 区间；每个样本具备发作期或发作间期标签，发作期样本至少包含片段内 onset 标注或可由训练集标注文件推导得到。若实际数据格式不同，应在数据读取层和标签映射层进行适配，不改变核心算法框架。

## 二、任务难点与设计原则

SEEG 信号具有高维、多通道、非平稳和个体差异显著的特点。与头皮 EEG 相比，SEEG 可直接记录深部脑区和皮层局部电活动，信噪比和空间分辨率较高，但通道数量、植入位置、命名方式和病灶分布因患者而异，跨受试者建模难度更高。癫痫发作起始阶段常表现为局部低幅快活动、节律性放电、频带能量突变或多通道同步增强，这些模式并非在所有患者中一致，且可能受到肌电、接触不良、电极漂移和工频干扰影响。

本任务不仅要求判断片段是否处于发作期，还要求给出发作概率、起始秒数和关键通道排序。因此模型设计不能仅追求片段分类准确率，还需要保留时间分辨能力和通道级可解释性。方案遵循以下原则：第一，训练与验证采用患者级隔离，避免同一患者数据同时出现在训练集和验证集导致性能虚高；第二，在类别不平衡条件下优先保证发作期召回率，并通过概率校准控制误报；第三，onset 定位以连续概率序列和变化点检测为基础，避免单窗口波动造成起始点漂移；第四，关键通道识别采用多证据融合，减少单一注意力权重的偶然性；第五，部署阶段优先采用可缓存、可裁剪、可量化的模型结构，控制单位样本推理时间。

## 三、数据预处理与样本构建

预处理流程应与临床 SEEG 分析习惯保持一致，同时避免引入无法在推理阶段复现的信息。首先将所有信号重采样至统一采样率。若原始采样率较高，可优先设定为 512 Hz 或 1 kHz；前者有利于推理速度，后者保留较完整的高频活动信息。若比赛评价更关注检测准确率，可采用 1 kHz 训练主模型，并通过蒸馏获得 512 Hz 轻量模型。

滤波方面，建议采用 0.5-150 Hz 带通滤波作为基础配置，并根据实际采样率保留 80-250 Hz 高频振荡扩展分支。工频干扰采用 50 Hz 及其倍频陷波处理，陷波范围应保持窄带，以免破坏发作相关频谱结构。坏道检测结合幅值异常、方差异常、平坦段比例、尖峰比例和与邻近通道相关性进行判定；明显坏道在训练中置零并记录 mask，推理时同样将异常通道排除出关键通道排序。

参考重构采用双路径策略。若数据提供原始参考，先转换为通道内稳健平均参考或双极导联；若通道命名包含电极轨迹和触点编号，可构造相邻触点双极导联以增强局部放电特征。由于不同患者电极布局差异较大，模型输入保留原始通道 ID 与有效通道 mask，不强行将通道映射到固定脑区。标准化采用患者内或样本内稳健标准化，即对每个通道使用中位数和四分位距进行归一化：

```text
x_norm = clip((x - median(x)) / (IQR(x) + eps), -8, 8)
```

相较均值方差标准化，该方法对尖峰和短时高幅伪迹更稳健。

样本构建采用多尺度滑窗。片段级分类模型的基础窗口可设为 4-8 秒，步长 0.5-1 秒；onset 定位分支可使用 1 秒或 2 秒短窗获得更高时间分辨率。训练标签通过窗口与发作标注区间的重叠比例确定：重叠比例超过阈值的窗口标记为发作期，完全位于发作间期的窗口标记为非发作期，处于边界附近的窗口用于 onset 分支训练或作为软标签样本。验证集划分必须以患者为单位；若患者数量不足，可采用 leave-one-patient-out 或 group k-fold，禁止同一患者不同发作片段跨训练集和验证集。

数据增强围绕跨患者泛化和抗伪迹能力展开。时域增强包括随机裁剪、时间平移、幅值缩放、高斯噪声、短时遮挡和通道 dropout；频域增强包括窄带扰动、轻微频谱衰减和随机陷波；通道增强包括随机屏蔽低贡献通道、模拟通道缺失和通道顺序扰动。对于发作期样本，可在不改变 onset 位置标注的前提下进行轻微时间平移。Mixup 可用于片段级分类，但不宜直接用于 onset 训练，以免产生不符合生理含义的起始点标签。

## 四、发作状态识别模型设计

本方案建议采用“时域卷积-时频增强-通道图注意-时序编码”的联合模型。设输入张量为 `X in R^(B x C x T)`，其中 `B` 为批大小，`C` 为有效通道数，`T` 为时间采样点数；同时输入通道 mask `M in R^(B x C)`，用于处理不同样本通道数量不一致的问题。模型由五个部分组成。

第一部分为局部时域特征提取模块。该模块采用深度可分离一维卷积和多尺度卷积核，分别覆盖尖波、节律性活动和低幅快活动等不同时间尺度。卷积核长度可设置为 16、32、64 和 128 个采样点，并在通道内共享，以减少参数量。该模块的作用是从原始波形中提取局部放电形态，避免过早依赖手工频域特征。

第二部分为时频特征增强模块。可采用可学习滤波器组或轻量化短时傅里叶变换分支，将信号投影至 δ、θ、α、β、低 γ、高 γ 和高频振荡相关频段。相比单纯 CNN，时频分支能够显式捕获发作起始阶段常见的频带能量迁移，尤其适合 SEEG 中局部高频活动增强的检测。为控制计算量，频域分支不需要保留完整谱图，可对每个窗口输出频带能量、频带能量变化率和谱熵等紧凑特征，再与时域卷积特征融合。

第三部分为通道间关系建模模块。SEEG 通道之间并非规则网格结构，直接使用二维卷积并不合适。方案采用基于特征相似度和通道相关性的动态图注意力层：模型在每个样本内根据通道嵌入计算通道邻接权重，并通过 mask 排除无效通道。该设计可学习发作传播过程中不同通道之间的同步变化，又不依赖固定电极布局。若提供电极空间位置，可将空间距离作为先验边权，与数据驱动相关性共同形成图结构。

第四部分为长程时序依赖建模模块。建议采用 TCN 或轻量 Transformer。TCN 通过膨胀卷积扩大感受野，推理稳定、易部署；Transformer 对长程依赖表达更强，但计算量较高。竞赛主模型可使用 4-6 层 TCN 与局部自注意力结合，增强模型对发作演化过程的建模能力。若片段较长，可先将原始时间轴压缩为帧级特征序列，再进行时序编码，以降低计算开销。

第五部分为发作概率输出头。模型输出窗口级发作概率 `p_t` 和片段级发作概率 `P`。片段级概率采用注意力池化或 top-k pooling 由窗口概率聚合得到，以适应发作只占片段部分时间的情况。损失函数采用加权 BCE 与 Focal Loss 的组合：

```text
L_cls = alpha * BCE(y, P) + beta * FocalLoss(y, P)
```

其中发作期样本权重高于发作间期，以缓解类别不平衡。若提供 onset 标注，可增加帧级监督损失 `L_frame`，约束模型在发作起始前后形成清晰概率跃迁。总损失为：

```text
L = L_cls + lambda_1 * L_frame + lambda_2 * L_calibration
```

概率校准采用温度缩放或验证集 Platt scaling，使输出概率更适合比赛要求的 6 位小数概率提交。训练策略采用 AdamW 优化器、余弦退火学习率、患者级 group k-fold 验证和早停机制。主模型可进行 3-5 折集成；最终部署模型再通过蒸馏压缩为单模型。

## 五、发作起始点定位方法

onset time 定位不宜直接由片段级分类结果推断，而应基于滑窗或帧级概率序列。推理时对输入片段按短窗进行密集滑动，得到时间序列 `p_1, p_2, ..., p_n`，每个概率对应窗口中心时间。首先对概率序列进行中值滤波或指数滑动平均，降低单窗波动：

```text
s_t = gamma * p_t + (1 - gamma) * s_(t-1)
```

随后采用阈值滞回策略定位发作开始。设高阈值 `theta_high` 用于确认发作，低阈值 `theta_low` 用于回溯起点。当连续 `K` 个窗口满足 `s_t >= theta_high` 时，判定存在稳定发作证据；再从该位置向前回溯，寻找最后一个 `s_t < theta_low` 的时刻，其后第一个超过低阈值的窗口作为候选 onset。该方法比单一阈值更稳健，可减少噪声尖峰导致的提前定位。

为进一步提高边界精度，可引入变化点检测。对平滑概率序列计算一阶差分 `d_t = s_t - s_(t-1)`，并结合频带能量变化率 `e_t` 构造起始评分：

```text
O_t = w_p * s_t + w_d * ReLU(d_t) + w_e * e_t
```

候选 onset 在满足连续阳性约束的前提下，选择 `O_t` 首次超过阈值的位置。最终 onset time 由窗口起点或中心点换算为秒数，并限制在输入片段范围内。若模型判定为发作期但概率序列未形成稳定连续阳性，则采用片段中 `O_t` 最大点作为 fallback，同时降低 onset 置信度。若片段级预测为发作间期，则 onset time 置为空值或按提交格式填充约定值。

## 六、关键通道识别策略

关键通道识别的目标是输出对发作判定贡献最大的 10 个 SEEG 通道 ID，并使排序具有稳定性和临床可读性。单纯使用注意力权重可能受到模型结构和归一化方式影响，因此本方案采用多证据融合评分。

首先提取模型内通道注意力得分 `A_c`。图注意力层和通道池化层均可产生通道权重，对时间维度求平均并按有效通道归一化。其次计算梯度归因得分 `G_c`，使用 Grad-CAM、Integrated Gradients 或输入梯度乘输入的方法，衡量通道信号变化对发作概率的影响。第三，进行遮挡检验得到 `O_c`：推理时逐一将单通道置零或替换为该通道基线噪声，观察发作概率下降幅度。第四，计算频带能量变化贡献 `F_c`，重点关注发作起始前后 β、γ 及高频活动能量增幅。

融合评分定义为：

```text
S_c = w_a * norm(A_c) + w_g * norm(G_c) + w_o * norm(O_c) + w_f * norm(F_c)
```

其中 `norm` 表示在有效通道内的稳健归一化，默认权重可设为 `w_a=0.25, w_g=0.30, w_o=0.30, w_f=0.15`。若推理时间受限，可在正式提交中使用注意力、梯度和频带能量三项，遮挡检验仅用于离线验证；若比赛允许较长推理时间，可保留遮挡检验以增强排序可靠性。最终排除坏道、低信噪比通道和缺失通道，按 `S_c` 从高到低输出前 10 个通道 ID。

为提高稳定性，通道贡献不使用单窗口结果，而是在高发作概率窗口及 onset 前后若干秒内聚合。若预测为发作间期，则仍输出对模型判定最有贡献的通道，但应以全片段平均贡献为依据。临床可读性方面，输出通道时保留原始通道 ID，不对通道重新编号；若数据包含电极轨迹名称，可按轨迹和触点顺序辅助检查排序是否集中于合理区域。

## 七、推理与部署方案

推理流程包括数据读取、预处理、滑窗推理、概率融合、标签判定、onset 定位、通道排序和结果格式化。系统首先读取单个样本的 SEEG 矩阵及通道 ID，进行采样率适配、滤波、陷波、坏道检测和标准化。随后按固定窗口长度和步长生成窗口序列，输入模型得到窗口级概率、片段级概率和通道贡献中间量。

片段级概率采用窗口概率的加权融合：

```text
P = max(mean_topk(p_t), attention_pool(p_t))
```

该策略可兼顾短暂发作证据和整体片段稳定性。标签判定使用验证集确定的阈值 `theta_cls`，并根据比赛指标偏好调整。若更重视召回率，可适当降低阈值；若误报惩罚较高，则采用更保守阈值并结合连续阳性约束。概率输出保留 6 位小数。

结果格式可按如下字段组织：

```text
sample_id, label, seizure_probability, onset_time, top10_channels
```

其中 `label` 为 0 或 1，`seizure_probability` 为 6 位小数，`onset_time` 在发作期输出秒数，非发作期按提交规则置空或填充固定值，`top10_channels` 按贡献度从高到低排列。

部署方面，GPU 路径适用于离线批量推理，可使用 PyTorch、TorchScript 或 ONNX Runtime GPU；CPU 路径适用于低资源环境，优先采用 TCN 主干、深度可分离卷积和 int8 量化模型。为降低延迟，预处理滤波器系数预先缓存，滑窗之间重叠部分的卷积特征可复用，通道贡献中的遮挡检验可在轻量部署版本中关闭。异常输入处理包括采样率不匹配、通道缺失、全零通道、极端幅值和片段长度不足；对长度不足样本采用镜像填充或零填充，并在 mask 中标识无效区域。

## 八、模型轻量化与算力优化

模型轻量化以保持发作期召回率和 onset 定位误差可控为前提。首先采用结构轻量化，将标准一维卷积替换为深度可分离卷积，将全局 Transformer 替换为局部注意力或 TCN，将完整频谱分支压缩为少量频带统计特征。其次进行知识蒸馏，以多折集成主模型作为教师模型，训练单个学生模型拟合教师的片段概率、窗口概率和通道贡献分布。蒸馏损失包括软标签 KL 散度、真实标签 BCE 和帧级概率 MSE。

剪枝方面，采用结构化通道剪枝和卷积核剪枝，优先删除低贡献滤波器和冗余时序层。剪枝后进行短周期微调，观察 AUC、Recall 和 onset MAE 是否下降。量化方面，优先采用动态量化或量化感知训练，将线性层和卷积层压缩至 int8；对幅值敏感的首层卷积可保留 fp16 或 fp32，以减少低幅快活动损失。低秩分解适用于较大的线性投影层，可将矩阵分解为两个低秩矩阵，降低参数量。

部署格式建议按目标环境选择。NVIDIA GPU 环境使用 ONNX + TensorRT，可结合 fp16 和 int8 校准；通用 CPU 环境使用 ONNX Runtime 或 OpenVINO；保留 PyTorch 环境时使用 TorchScript 以减少 Python 调度开销。推理优化还包括滑窗批处理、重叠窗口缓存、坏道提前剔除、top-10 通道候选集预筛选和按需计算解释模块。最终建议提交模型分为主模型和轻量模型两版：主模型用于追求最高检测性能，轻量模型用于时延约束严格场景；若比赛综合评分显著考虑时延，则以蒸馏后的轻量模型作为最终提交模型。

## 九、训练验证与消融实验设计

实验设计分为 baseline、增强模型和轻量模型三类。Baseline 使用标准滤波、稳健标准化、TCN 分类器和简单阈值 onset 定位；增强模型加入时频分支、通道图注意力、帧级 onset 监督和多证据通道归因；轻量模型通过蒸馏、剪枝和量化获得。三类模型在同一患者级划分下评估，避免数据划分差异导致结论不可比。

消融实验应覆盖关键技术环节。预处理消融比较原始参考、平均参考和双极参考；模型结构消融比较 CNN、TCN、TCN+注意力、时频增强模型；通道建模消融比较无通道关系、静态相关图和动态图注意力；onset 后处理消融比较单阈值、阈值滞回、变化点检测及联合评分；通道归因消融比较注意力、梯度、遮挡和融合评分。轻量化消融比较 fp32、fp16、int8、剪枝和蒸馏组合。

建议评估指标包括 AUC、F1、Recall、Specificity、Precision、Balanced Accuracy、onset MAE、onset median error、单样本推理时间、峰值显存或内存、参数量和 FLOPs。若比赛采用综合评分，应根据官方验证反馈调整阈值和模型版本。对于医学检测任务，发作期漏检通常比误报更敏感，因此模型选择应在保证召回率的基础上优化特异性和时延。

## 十、预期优势与风险控制

本方案的优势在于将发作状态识别、onset 定位和关键通道解释统一到同一时序建模框架内，避免分类模型、定位模型和解释模块彼此割裂。时域卷积保留原始波形形态，时频增强补充频带能量变化，通道图注意力适配 SEEG 非规则电极布局，TCN 或局部注意力模块刻画发作演化过程。关键通道排序采用注意力、梯度、遮挡和频带能量多证据融合，较单一权重更稳定。轻量化方案以蒸馏和结构化压缩为主，便于在 CPU 或普通 GPU 环境中部署。

主要风险包括五类。第一，训练数据规模不足可能导致深度模型过拟合。应采用患者级交叉验证、强正则化、数据增强和模型蒸馏缓解。第二，跨患者泛化不足可能导致验证集性能波动。应避免依赖固定通道位置，使用通道 mask、动态图建模和稳健标准化。第三，通道缺失或坏道可能影响关键通道排序。应在预处理阶段记录通道质量，并在排序阶段剔除无效通道。第四，伪迹干扰可能导致误报。应结合幅值异常检测、频谱异常检测和连续阳性约束降低单点误判。第五，onset 标注不一致可能影响定位模型训练。应采用软标签、边界容忍损失和后处理回溯策略，避免过度拟合单一标注边界。

## 十一、最终技术方案摘要

本方案以 SEEG 多通道时序信号为对象，建立由稳健预处理、时频联合特征提取、通道关系建模、长程时序编码、起始点后处理和通道贡献融合组成的发作状态智能检测流程。模型以 `C x T` 多通道信号及有效通道 mask 为输入，通过多尺度时域卷积提取局部放电形态，通过频带增强模块捕获发作相关能量变化，通过动态图注意力学习通道间协同关系，并以 TCN 或局部注意力结构建模发作演化过程。推理阶段输出片段级发作概率和二分类标签，利用窗口级概率序列完成 onset time 定位，并基于注意力、梯度、遮挡和频带能量贡献形成 top-10 关键通道排序。部署阶段通过蒸馏、剪枝、量化和滑窗缓存降低参数量与推理延迟，使方案在保证检测精度和解释稳定性的同时具备竞赛提交和工程落地条件。
